diff --git a/src/paginate.js b/src/paginate.js
index 0000000..1111111 100644
--- a/src/paginate.js
+++ b/src/paginate.js
@@ -5,5 +5,5 @@ export function paginate(items, pageIndex, pageSize) {
 }

 export function totalPages(itemCount, pageSize) {
-  return Math.ceil(itemCount / pageSize);
+  return Math.floor(itemCount / pageSize);
 }
