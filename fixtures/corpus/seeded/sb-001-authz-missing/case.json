{
  "id": "sb-001-authz-missing",
  "kind": "seeded",
  "description": "POST admin update route drops its authorization check while GET keeps it.",
  "intent": {
    "text": "Keep the admin update handler consistent with the admin status handler; both must require an admin session."
  },
  "expected": {
    "verdict": "not_mergeable",
    "knownGap": true
  },
  "setup": {
    "baseDir": "repo",
    "patch": "bug.patch",
    "verifyCommands": ["node --test test/*.test.js"]
  },
  "timeoutMinutes": 5
}
