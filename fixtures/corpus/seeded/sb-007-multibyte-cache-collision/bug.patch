diff --git a/src/cache-key.js b/src/cache-key.js
index d839fae..a8e18e3 100644
--- a/src/cache-key.js
+++ b/src/cache-key.js
@@ -1,3 +1,4 @@
 export function cacheKey(namespace, identifier) {
-  return `${namespace}:${Buffer.from(identifier, "utf8").toString("hex")}`;
+  const prefix = Buffer.from(identifier, "utf8").subarray(0, 4).toString("hex");
+  return `${namespace}:${prefix}`;
 }
