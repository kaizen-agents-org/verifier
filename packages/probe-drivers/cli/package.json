{
  "name": "@verifier/probe-driver-cli",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "pretest:package-entry": "pnpm build",
    "test": "vitest run",
    "test:package-entry": "node --test test/package-entry.test.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@verifier/probe-sdk": "workspace:*"
  }
}
