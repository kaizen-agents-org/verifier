{
  "name": "@verifier/agents",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "eval": "node --import tsx src/eval/run.ts",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.112.3",
    "@verifier/core": "workspace:*",
    "@verifier/probe-driver-api": "workspace:*",
    "@verifier/probe-driver-cli": "workspace:*",
    "@verifier/probe-sdk": "workspace:*",
    "ajv": "^8.20.0",
    "zod": "^3.25.0"
  }
}
